function displayInfo(selectedYear) {
  //Update elements
  let displaySelectedYear = d3.select('.selected-year').text(selectedYear);

  let displayCountyName = d3.select('.county-name').text('countyName');

  // let displayYear = d3.select('.county-pop-year').text(countyName);
  // let displayCountyPop = d3.select('.county-pop-year');

}
